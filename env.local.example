## Copy this file to ".env.local" (create it in the project root)

## Option A (recommended): single connection string
# DATABASE_URL=postgresql://USERNAME:PASSWORD@localhost:5432/DATABASE_NAME

## Option B: individual settings (used if DATABASE_URL is not set)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=eauctionuser
# PGPASSWORD=REPLACE_ME
# PGDATABASE=eauctiondb

